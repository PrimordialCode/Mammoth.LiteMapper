# Markdown Style

Write tracker text in readable Markdown.

- Use short headings for separate topics.
- Use bullets for multiple facts, steps, risks, or validation results.
- Leave blank lines between sections.
- Use fenced code blocks for commands, logs, JSON, or stack traces.
- Avoid dense paragraphs that combine many labels such as `Outcome: ... Steps: ... Validation: ...`.
