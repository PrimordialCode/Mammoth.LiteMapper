# Resume prompt

Resume the all-defects goal in `C:\Work\Mammoth.LiteMapper`.

Latest checkpoint supersedes older counts below: CR-015 declarations/configuration and read-only-field repairs pass398 default Roslyn4.8 generator tests. All sessions are terminal and4.8 is selected. Run current-revision newer hosts, restore4.8, full solution, and Linux AOT serially. Then prove CR-016 missing-type/unusable external-registration findings and continue remaining audits. Constructor fixture/runtime gaps listed in the latest HANDOFF are now repaired; do not recreate them. No new normative blocker exists.

PENDING-0005 is approved, incorporated, and implemented: reject inconsistent named composites with LITEMAPPER7002 (target Both16 conflicts with mapped Read4 OR Write8=12). Do not ask again. Current evidence:39 focused enum/nullable passes;368 passes on each Windows Roslyn host;418 full Windows passes with one prerequisite skip; required Linux AOT one pass/zero skips. Default4.8 is restored and all sessions are terminal. PENDING-0002/0003/0004 also remain approved. Follow latest HANDOFF/STATUS; earlier counts below describe preceding revisions. Next prove/fix CR-015 valid attribute-cast and enclosing-type findings, correct misleading constructor fixtures, and continue remaining coverage/performance/Linux matrix review.

Read `docs/HANDOFF.md`, `AGENTS.md`, the complete `SPECIFICATION.md`, `IMPLEMENTATION_PLAN.md`, `DECISIONS.md`, `STATUS.md`, and `docs/CONFORMANCE_REVIEW.md`. Inspect the existing dirty worktree and preserve all changes, including my `AGENTS.md` and `.tokensave/config.json` edits.

Continue the already approved whole-project review and defect repair across milestones. PENDING-0002/0003/0004 are approved and incorporated; do not ask me to approve them again. Add failing MSTest regressions before fixes. Stop only for a genuinely new normative ambiguity, record exact specification references, and ask for a decision. Always clarify findings and proposed behavior with a concrete example.

Use the latest dated checkpoint in `docs/HANDOFF.md`. On 2026-09-09, after CR-013/CR-004 repairs, Windows Roslyn4.8.0/4.14.0/5.9.0 suites each passed320 tests and default4.8 was restored. The full Windows solution passed370 tests with one local AOT prerequisite skip. Required isolated Linux Native AOT passed one test, zero failures/skips (27s). All validation sessions are terminal. Async declaration detection, package-feed provenance, inherited discovery, and converter-result conversions were repaired with failing regressions first. Do not repeat unchanged validation automatically; rerun relevant checks after new source changes. Record every failure, skip, blocked command, and actual result accurately.

The newer CR-010 checkpoint supersedes the counts above: nullable boxing and enum repairs now pass345 default-host generator tests, including oblivious annotations and generated-name collision regressions. Follow the latest HANDOFF validation state and poll any listed live handle before starting more builds. Current-revision newer-host, full solution, and Linux AOT evidence must be checked there; do not assume older results validate the new source. Then continue CV-003/004 Linux matrix, CV-007 performance, and CV-009 feature/diagnostic audit. For example, From.Ready=1 now maps by name to To.Ready=9 with nullable enum types; those regressions are executed and repaired. Do not recreate CR-004/010/013 fixes.

Keep usage, skill, decisions, plan, status, and review evidence synchronized. `docs/USAGE.md` must remain a consumer usage guide with examples, without implementation tracking or conformance/handoff links. Use bounded delegation where useful, but serialize shared builds/restores. Do not commit, push, publish, or declare the goal complete while defects or required validation remain.
Latest override (2026-09-09): platform validation for CR-015/read-only repairs is complete:398 tests per Windows/Linux Roslyn4.8/4.14/5.9 host,448 Windows solution passes with one missing-linker AOT skip, required Linux AOT one pass/zero skips. Default4.8 restored; no sessions active. Read latest HANDOFF/STATUS before historical instructions below. Continue CR-016 failing registrations, nested-update and22.11–22.16 evidence gaps, Source Link advisory repair, Linux consumer matrix/remote CI/performance. SourceLink.GitHub10.0.300 currently resolves vulnerable Build.Tasks.Git10.0.300;10.0.303 is the verified advisory patch candidate, not yet applied. Preserve all changes and approved decisions. Do not repeat unchanged validation without a new change or unresolved concern.
Latest override: CR-016 and Source Link repairs are implemented. Final default4.8 generator429 passes (13:13:34), three dependency regressions pass with resolved patched10.0.303. Newerhost427 passes predate final ref-return correction; rerun newerhosts/fullWindows/Linux/AOT on429 revision. All sessions terminal, default4.8 selected. Read newest HANDOFF/STATUS. Continue CR-017 nested updates and remaining coverage/platform/performance gaps afterward; no new normative blocker.
Latest override: CR-016 platform validation completed:429 per Windows/Linux host; Windows full482/one linker skip. Linux full482/one helper failure, then repaired warning helper8 focused passes and full Linux packaging56 passes/no skips. Local12-consumer matrix and AOT verified. All sessions terminal/default4.8. Read newest HANDOFF/STATUS; proceed to failing CR-017 nested-update regressions and repair, remaining feature/performance review. Remote CI still unexecuted. Do not repeat unchanged validation merely because older instructions below say pending.
